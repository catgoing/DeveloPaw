<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Store">

	<insert id="insertProducts" parameterType="Store">
		INSERT INTO STORE_PRODUCTS (P_ID, P_PICK, P_NAME, P_TYPE, P_CATEGORY, P_CONTENT,
									PRICE, STOCK, S_IMAGE, L_IMAGE)
		VALUES( 
		 P_SEQ.NEXTVAL, #{p_pick}, #{p_name}, #{p_type}, #{p_category}, #{p_content}, 
		 #{price}, #{stock}, #{s_image}, #{l_image})
	</insert>
	
	<!-- String p_id로 products검색 -->
	<select id="getProducts" parameterType="Store" resultType="Store">
		SELECT * FROM STORE_PRODUCTS
		WHERE P_ID = #{p_id }
	</select>
</mapper>









